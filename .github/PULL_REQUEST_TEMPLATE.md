## Description

<!-- Describe the changes in this PR. -->

## Type of change

- [ ] feat — New feature
- [ ] fix — Bug fix
- [ ] docs — Documentation only
- [ ] style — CSS/formatting (no functional change)
- [ ] refactor — Code restructuring (no functional change)
- [ ] perf — Performance improvement
- [ ] test — Tests only
- [ ] chore — Build, config, dependencies
- [ ] ci — CI/CD changes

## RTM References

<!-- List the RTM requirement IDs this PR addresses. -->
- REQ-

## Checklist

- [ ] PHPCS passes (`composer phpcs`)
- [ ] ESLint passes (`npm run lint:js`)
- [ ] Stylelint passes (`npm run lint:css`)
- [ ] All text is Dutch (`lang="nl-NL"`)
- [ ] All strings are internationalised (`__()`/`_e()` with `'hds'` domain)
- [ ] All output escaped (`esc_html()`, `esc_attr()`, `esc_url()`)
- [ ] All inputs sanitised
- [ ] No hardcoded URLs or phone numbers (use `get_theme_mod()` or `hds_get_*()`)
- [ ] Semantic HTML is used
- [ ] All images have `alt` text
- [ ] Mobile-first CSS is used (`min-width` queries)
- [ ] No `console.log()` in production code

## Screenshots

<!-- If applicable, add screenshots of the changes. -->

## Testing instructions

<!-- How can a reviewer test this? -->

1.
2.
3.

## Dependencies

<!-- List any dependencies this PR introduces. -->
